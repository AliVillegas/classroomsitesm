version: 1
applications:
  - frontend:
      phases:
        build:
          commands: npm run build
      artifacts:
        baseDirectory: /client/
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: /
